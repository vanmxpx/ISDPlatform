export class User {
    id: number;
    nickname: string;
    email: string;
    description: string;
    photoURL: string;

    // IsVerified: boolean;
    // IsCreator: boolean;
    // IsBanned: boolean

    // platformLanguage: string;
    // platformTheme: string;
}
